# DECISIONS.md — 정적 스택 적응 결정 기록

원문 기획서(12차 4종: 오늘 노을 / 1회당 / 레시피 계보 / 신세)는 **Next.js + Supabase +
서버 크론 + 웹푸시**를 전제로 쓰여 있다. 이 저장소는 **빌드 없는 정적 HTML**이고
`CLAUDE.md` 규칙상 새 앱은 "단일 파일 완결형, 백엔드·외부 API·키 금지"다.

아래는 그 간극에서 내린 결정과 근거다. **원칙: 못 하는 것을 하는 척하지 않는다.**
못 하는 기능은 빼거나 정직한 대체물로 바꾸고, 그 사실을 앱 화면에 그대로 적었다.

---

## 공통

| 원문 요구 | 결정 | 근거 |
|---|---|---|
| Supabase 테이블 | **localStorage JSON**으로 1:1 매핑 | 서버·계정 없음. 스키마 구조(관계·인덱스 의미)는 그대로 유지 |
| 로그인/계정 | 미구현 | 인증 서버 필요. 전 앱이 기기 로컬 전용 |
| 서버 크론·배치 | 미구현 | 실행 주체 없음 |
| 웹푸시 | 미구현 → 앱별 대체 | VAPID 키 + 푸시 서버 필요 |
| 데이터 백업 | **JSON 내보내기/가져오기** 전 앱 제공 | 로컬 저장은 브라우저 데이터 삭제 시 소실 |
| 광고 | 결과 하단 1개만 (`adsbygoogle`) | `CLAUDE.md` 콘텐츠 정책 |

---

## 1. 오늘 노을 (`/sunset-forecast/`)

### 기상 데이터 자동 수집 — **미채택**
원문 Phase 0의 핵심(기상청 단기예보 API)이지만 정적 스택에서 불가능하다고 판단했다.

- **기상청 / 에어코리아(data.go.kr)**: 서비스 키 필수. 정적 페이지에 키를 넣으면
  클라이언트에 그대로 노출되어 즉시 도용·한도 소진된다. 프록시하려면 서버가 필요하다.
- **키 없는 대안(Open-Meteo 등)**: 무료 티어가 **비상업 이용 한정**이다. 이 사이트는
  AdSense를 게재하는 상업 사이트라 약관 위반 소지가 있어 채택하지 않았다.
- **결론**: 구름·강수·대기·습도는 **사용자가 보고 고르는 입력값**으로 받는다.
  화면과 FAQ에 "이 페이지는 기상 데이터를 자동으로 받아오지 않는다"를 명시하고
  기상청 날씨누리 링크를 제공한다.

### 층별 운량 확보 여부 (원문 DoD 항목)
- 기상청 공개 API의 단기예보는 **전운량(SKY/총 하늘상태)** 중심이고, 중·상층운과
  하층운을 나눈 값은 일반 공개 항목이 아니다. 즉 **API를 붙였더라도 층별 운량은
  그대로 얻지 못한다.**
- 대체 지표로 원문이 제시한 "중·상층운 비율"을 사용자 관측 5단계
  (거의 없음 / 20% / 50% / 75% / 95%)로 받는다. **한계: 사람의 눈대중이라
  객관 수치가 아니다.** 이 한계를 앱 안에 적어 두었다.

### 점수 모델
- **LLM 호출 0건.** 순수 가중합.
- 가중치는 코드에 흩어 두지 않고 `MODEL` 설정 객체 **한 곳**에서만 온다.
  결과 화면의 "가중치 표 보기"에서 사용자가 전부 펼쳐 볼 수 있고 `MODEL_VERSION`
  (`v1.0-hypothesis`)이 함께 표시된다.
- 중·상층운은 단조 증가가 아니라 **50% 최고점 산 모양 곡선**(원문 가설 그대로).
- 근거 3줄은 요소별 기여도 순으로 자동 생성한다.

### 정직성 / 적중률
- 서버 집계가 없으므로 **이 브라우저의 평가 기록**만으로 계산한다.
- 표본 5건 미만이면 숫자를 만들지 않고 "아직 모른다"고 적는다.
- 70점 이상이라 한 날 vs 70점 미만이라 한 날의 실제 만족 비율을 나란히 보여 주고
  **차이가 0 이하이면 "아직 무작위보다 낫다는 근거가 없습니다"라고 그대로 표시**한다.
- 헤더에 `🧪 실험 중` 라벨 상시 노출.

### 알림 — **.ics 캘린더로 대체**
- 웹푸시 불가 → 절정 예상 시각의 일정 파일을 내려받고, `VALARM TRIGGER:-PT90M`으로
  **일몰 90분 전 1회** 울리게 한다. 원문의 발송 정책(1회, 좋을 때만)을 그대로 지킨다.
- "오늘은 별로일 것 같습니다" 알림은 만들지 않는다(사용자가 낮은 점수 날엔 파일을
  받지 않으면 그만).

### 정확한 부분 (외부 데이터 0건으로 계산)
- 일출·일몰·남중·골든아워·블루아워·시민박명, **일몰 방위각**을 태양 위치 계산식
  (SunCalc 계열 근사)으로 직접 계산.
- 검증: 서울 하지 05:12/19:58, 동지 07:44/17:18, 춘분 06:38/18:44(방위 270.2°) —
  한국천문연구원 공표값과 **±1분** 이내 일치.
- 사진 업로드는 구현하지 않았다(EXIF·GPS 저장 0건이 자동 충족).

### 미구현 — 소유자 승인 필요
지역 랭킹("오늘 전국에서 가장 좋은 곳"), 자동 예보 배치, 진짜 푸시 알림, 서버 기반
전체 사용자 적중률 집계. 전부 서버·API 키 도입 시에만 가능.

---

## 2. 1회당 (`/cost-per-wear/`)

### 지킨 절대 원칙 (원문 DoD)
- **신체 관련 필드 0개** — 사이즈·치수·체형·체중·핏 입력란이 존재하지 않는다.
- **제휴·쇼핑 링크 0건** — 외부 링크는 canonical 외에 하나도 없다(감사 완료).
- 죄책감 카피 없음. 1회당 원가가 높은 항목을 "낭비"라 부르지 않고,
  미착용 목록 이름도 "요즘 안 입은 것"이다.
- **스트릭 없음**(원문 지시).

### 사진 등록 — **이모지 타일로 대체**
원문은 "사진 1장 + 가격 + 시기"를 등록 최소 단위로 잡았다. 그러나 localStorage는
대략 5MB가 상한이라 사진 몇십 장이면 저장소가 차고 **기록 전체가 깨진다.**
IndexedDB로 우회할 수 있으나 단일 파일 앱의 복잡도가 크게 올라간다.
→ 카테고리 이모지 타일로 대체했다. 인식 속도는 오히려 빠르고, 등록 마찰은 더 낮다.
FAQ에 이유를 적었다.

### 구현한 것
- 1회당 원가 = 구매가 ÷ 착용 횟수. **미착용은 무한대가 아니라 "0회"** 표시(원문 지시).
- 구독(`kind=sub`)은 **그 달 요금 ÷ 그 달 사용 횟수**로 변형 계산.
- 카테고리는 하드코딩된 옷 전용이 아니라 가전·취미장비·구독까지 포함한 테이블.
- 3초 기록: 홈 상단 8개 타일(자주+최근 정렬) 원탭, 재탭 시 취소, `(item,날짜)` 중복 방지.
- 소급 입력: 오늘/어제 토글.
- 통계: 옷장 총액, 총 착용, 평균 1회당, **착용 편중도(상위 20% 비중)**, 30일 미착용, 0회 목록.
- 명예의 전당 5위, 목표 1회당 → 남은 착용 횟수.
- 방출 = 삭제가 아니라 아카이브(최종 1회당 원가 보존) → 카테고리별·가격대별 평균 착용 패턴.
- 공유 카드/링크에는 **숫자와 카테고리만**. 이름 포함은 사용자가 체크박스로 켜야 한다.

### 미구현
위젯·잠금화면 바로가기(네이티브 앱 필요), 기기 간 동기화(서버 필요),
오프라인 동기화 큐(단일 기기 로컬 저장이라 애초에 오프라인 동작이 기본값).

---

## 3. 레시피 계보 (`/recipe-tree/`)

### LLM 구조화 파싱 — **규칙 기반 파서로 대체**
원문은 "Claude API로 구조화 파싱"을 지정했으나 유료 키 + 서버 프록시가 필요하다.
→ **정규식 + 단위 사전(28종) + 섹션 헤더 인식**으로 파싱한다.
원문이 강조한 "파싱 결과를 그냥 저장하지 마라"는 그대로 지켰다 — 파싱 결과는 항상
편집 표에 채워지고 사용자가 확인·수정한 뒤에만 저장된다. 화면에 "AI 아님"을 명시.

### 저작권 방어
- 등록 시 출처 선택 **필수**(직접 개발 / 가족·지인에게 배움 / 참고 후 변형).
  "참고 후 변형"은 출처 표기 없이는 저장이 막힌다.
- 붙여넣기 감지: 길이·문체·중복·출처 문구·링크·저작권 표기 등 9개 정황 검사.
  **테스트 10종 양성 / 3종 음성 통과**(`scripts/test-recipe-diff.mjs`).
- **사진 업로드 기능 자체를 넣지 않았다** — "본인 촬영본만"을 강제할 방법이 없으므로
  아예 받지 않는 쪽이 안전하다.

### fork / diff / 계보
- 서버가 없으므로 **레시피 전체가 담긴 링크(`?r=`)가 곧 공유 단위**다.
  링크를 가족에게만 보내면 그게 원문의 "가족 모드"다(별도 그룹 기능 불필요).
- 포크 시 **부모 1대의 재료·단계 스냅샷**을 자식에 함께 저장 →
  원문 요구대로 부모가 삭제돼도 자식의 diff·계보가 살아남는다.
- 조상은 요약(제목·작성자·변경 이유)만 최대 6대까지 이어 붙여 URL 크기를 억제.
- diff는 개발자 diff가 아니라 사람 문장: `설탕 1큰술 → 매실청 2큰술`.
  1:1 교체는 화살표 한 줄로 합치고, 추가/삭제/순서변경/시간변경을 구분한다.
- **변경 이유 한 줄(140자) 없이는 포크 저장 불가.** 차이가 0건이어도 저장 불가.

### "이 레시피는 N번 갈라졌습니다" — 반쪽만 구현
서버가 없어 **다른 사람의 포크 수를 알 수 없다.** 지어내지 않고
"내 기기에서 N번 갈라졌습니다"로 표기하고, 왜 그런지 화면에 적었다.

### 원본 작성자 알림 / 검수 큐 / 개별 레시피 JSON-LD
전부 서버 필요 → 미구현. 개별 레시피는 클라이언트 렌더라 색인 대상이 아니므로
JSON-LD는 앱 단위(SoftwareApplication + FAQPage + BreadcrumbList)만 넣었다.

### 테스트 (원문 DoD)
`node scripts/test-recipe-diff.mjs` — **48개 전부 통과.**
테스트는 `recipe-tree/index.html`의 `@engine-start ~ @engine-end` 구간을
**그대로 추출해 실행**한다(코드 복제 없음 = 테스트 대상이 곧 배포 코드).
포함: 재료 6종 변경 / 단계 8개 변경 / 1:1 교체 / 순서 변경 / 시간 변경 /
붙여넣기 감지 13종 / 파서 10종 / **3단계 포크 계보 샘플 7종**.
계보도 렌더는 헤드리스 크로미움으로 3세대 샘플을 실제 렌더해 확인했다
(엄마 김치찌개 → 우리집 김치찌개(매실청) → 매운 김치찌개(청양고추)).

---

## 4. 신세 (`/sinse/`)

### 절대 원칙 — 코드 수준에서 강제
| 금지 항목 | 검증 |
|---|---|
| 상대에게 가는 알림 | `Notification` / `serviceWorker` / `PushManager` / 연락처 API **호출 0건** |
| 초대·공유 코드·상호 확인 | 기능 없음. 외부 링크는 canonical 외 0건 |
| 점수·등급·랭킹 | 없음. 사람별 화면은 받은 횟수·준 횟수를 나란히 적기만 함 |
| 적자/흑자 표현 | 없음 |
| 독촉·리마인더 | 없음. 1년 지난 기록은 **목록으로만**, 알림 없음 |
| 금액 환산·가중치 | 없음 |
| 연락처 권한 요청 | 없음. 호칭은 사용자가 직접 입력 |

### 앱 잠금(생체인증) — **의도적 미구현**
원문 Phase 4 항목이지만, 서버 없는 정적 페이지에서 만든 비밀번호/PIN 잠금은
**실질 보안이 되지 않으면서 안전하다는 착각만 준다**(데이터는 여전히 평문
localStorage에 있고 개발자도구로 바로 열린다). 넣지 않고, 대신 화면에
"기기 자체 잠금을 쓰라"고 안내했다. WebAuthn은 정적 스택에서 검증 서버가 없어 불가.

### 구현한 것
- 받았다/줬다, 프리셋 8종(밥·커피·태워줌·도와줌·챙겨줌·들어줌·선물·기타).
- 최근 상대 8명 칩 → 종류+상대 두 번 탭이면 기록 완료. 나머지는 나중에.
- 사람별(시간순) / 종류별(막대) / 오래된 것(1년 경과, 조용히).
- 갚음 표시는 느슨한 연결이고 **갚아도 기록은 사라지지 않는다**.
- 연말 정산 + 카드 PNG. **상대 이름은 기본 마스킹**(준○○), 켜야 노출.
- 감사 표현 도우미: 문장 초안 생성 + **복사만**. 앱이 대신 보내지 않는다.

---

## 소유자 결정이 필요한 것 (과금)

Supabase(또는 동급 백엔드) + 키 도입을 승인하면 가능해지는 것들:

1. 노을: 기상청 API 실연동, 자동 예보 배치, 진짜 웹푸시, 전체 사용자 적중률 집계, 지역 랭킹
2. 1회당: 기기 간 동기화, 사진 저장
3. 레시피 계보: LLM 구조화 파싱, 원본 작성자 알림, 전체 포크 수, 검수 큐,
   개별 레시피 SSR + Recipe JSON-LD(장기 SEO 자산)
4. 신세: 없음 — 이 앱은 서버가 생겨도 **로컬 전용을 유지하는 편이 맞다**
