# DECISIONS — 11차 아이디어 4종 구현 기록

원본 기획서(`ideasv11.md`)는 네 개 앱을 각각 Next.js/Supabase/pgvector, Expo(RN)+SQLite,
서버 그래프 라우팅, Next.js PWA + OCR API 스택으로 전제한다.
이 저장소는 `CLAUDE.md` 기준 **순수 정적 사이트(빌드 프레임워크·백엔드·외부 API·키 없음)** 이므로
그대로는 들어올 수 없다. 그래서 네 개 모두 **정적 축소판(reduced version)** 으로 구현했다.

기준으로 삼은 원칙:

1. 기획서 2번이 명시한 "축소 버전이 가능한지 검증하고, 불가능하면 멈춰라"를 네 개 전부에 적용.
2. 축소하면서 **없는 기능을 있는 것처럼 보이게 만들지 않는다.** 못 만든 부분은 앱 화면 안의
   "솔직하게 밝히는 한계" 문단으로 사용자에게 직접 고지한다.
3. 각 앱은 `/<slug>/index.html` 단일 파일 완결형. `gen-pages.mjs`의 `BUILTINS`에 등록해
   허브·사이트맵에 반영하고 생성기의 orphan 정리에서 보호한다.
4. VIRAL 5요소(URL 상태 / 공유 유입 CTA / canvas 카드 + 워터마크 / 공유 4종 / 리믹스)를 만족한다.

---

## 1. 이름 없는 것들 — `/nameless/`

**원안**: 현상 제출 → 다른 사용자가 이름 제안 → 7일 표결 → 최다 득표로 등재.
Next.js + Supabase + pgvector(임베딩 0.85 중복 검사) + LLM 검수 + 마감 배치.

**결정**

| 항목 | 원안 | 구현 | 근거 |
|---|---|---|---|
| 집단 표결·공용 등재 | 서버 집계 | **미구현** | DB·배치가 필요. 앱 안 "솔직하게 밝히는 한계" 문단에 명시 |
| 씨앗 50개 | DB 적재 | 등재어 30 + 미명 현상 18을 **HTML 본문에 정적 수록** | DB 없음. 본문에 있어야 색인도 됨 |
| 중복 검사(임베딩) | pgvector 0.85 | **미구현** | 임베딩 API·키 금지 |
| LLM 검수 | 서버 LLM | **클라이언트 규칙 기반 검수**(한글 2~8자 화이트리스트 + 집단지칭 접미사 + 블랙리스트 + PII/링크 정규식) | 외부 API 금지. 애매하면 반려가 기본값 |
| 신고 → 등재 취소 | 서버 큐 | **미구현**(등재 자체가 없으므로 대상 없음) | — |
| 내 기록 | 계정 | localStorage(`tec-nameless-mine`, 최대 30건) | 로그인 없음 |
| 결과 유통 | `/r/{slug}` | `?r=<base64>` URL 상태 + canvas PNG 카드 | 정적 스택 표준 패턴 |

**의도적으로 유지한 것**

- 사전 조판: 크림 지면(#f4efe3), 명조 계열 세리프, 620px 좁은 단, 3중선 제호, 「」 용례.
  "웹앱"이 아니라 "사전"으로 보이는 것이 이 제품의 절반이라는 기획서 판단을 그대로 따랐다.
- "아직 이름이 없는 것" 섹션을 **비워 둔 채로 노출**. 원안에서 가장 재미있다고 본 부분.
- 씨앗은 전부 `씨앗 편찬` 표기. 표준어가 아님을 등재어·카드·FAQ 세 곳에서 반복 고지.
- 하루 한 개 집중 노출 → 정적 축소판에서는 **날짜 해시로 "오늘의 현상"을 결정**해 대체.

**검증**

- 집단지칭·비하·욕설 이름 20종 전부 반려 (`test-moderation.mjs`, 통과).
- 형식 위반 10종(자모 단독·영문·숫자·기호·공백·1자·9자) 전부 반려, 정상 이름 8종 전부 통과.
- 까닭/명명자 칸의 전화번호·이메일·주민번호·URL 5종 전부 반려.
- inline JS 문법, JSON-LD 3종 파싱, 광고 유닛 1개, 외부 리소스 0개, 태그 균형 (`check-app.mjs`).

---

## 2. 오늘의 다섯 장 — `/five-photos/`

**원안**: Expo(RN) 또는 Capacitor로 사진첩 전체를 읽고, 하루 5장 스와이프 판정,
30일 유예함 후 OS 삭제 다이얼로그. 기획서가 "Phase 0에서 플랫폼 제약을 검증하고,
축소 버전조차 불가능하면 멈추라"고 지시한 앱이다.

**Phase 0 — 웹 플랫폼 제약 검증 결과**

| 능력 | 웹에서 가능한가 | 근거 |
|---|---|---|
| 사진첩 전체 열거 | **불가** | 브라우저에 사진 라이브러리 API가 없다. `<input type=file multiple>`로 사용자가 고른 것만 볼 수 있다 |
| 폴더 단위 읽기 | 부분 가능 | `webkitdirectory`(데스크톱), File System Access API는 Chromium 데스크톱 한정 |
| 사진 삭제 | **불가** | OS 사진첩에 대한 삭제 권한이 웹에 없다. FSA의 `removeEntry`는 사진첩이 아닌 임의 폴더 대상이고 파일별 OS 확인 다이얼로그도 없다 |
| 백그라운드 아침 알림 | **불가** | 푸시 서버 + Service Worker 필요. 이 저장소엔 서버가 없다 |
| 촬영일 | 대체 가능 | 처음엔 `File.lastModified`만 썼으나(메신저 사진에서 부정확), 리뷰 루프에서 **EXIF DateTimeOriginal**을 파일 앞 128KB만 읽어 파싱하도록 개선 |

→ **축소 버전은 성립한다**고 판단하고 진행했다. 이 제품의 핵심 가치("3만 장은 못 봐도
다섯 장은 본다" + "사진이 기기 밖으로 안 나간다")는 사용자가 파일을 고르는 방식으로도
그대로 살아 있고, 삭제만 "파일명 목록 내려받기"로 바뀌기 때문이다.

**결정**

| 항목 | 원안 | 구현 | 근거 |
|---|---|---|---|
| 사진첩 자동 열거 | PhotoKit/MediaStore | 사용자가 파일/폴더 선택 | 위 표 |
| 삭제 | OS 다이얼로그 | **삭제 경로 0개.** 유예 만료분 파일명 `.txt` 내려받기 | 권한 없음. 조용히 지우는 코드가 없다는 게 더 중요 |
| 아침 알림 | 로컬 알림 | **미구현**(화면에 이유 명시) | 흉내 내지 않음 |
| 위치 표시 | 대략적 위치 | **미구현** | 기획서의 "장소 분석 금지" 원칙을 우선 적용. GPS 태그는 읽지 않는다(리뷰 루프 2R에서 촬영일 태그만 읽는 EXIF 파서를 추가했고, 위치 태그 미접근을 테스트로 고정) |
| 로컬 DB | SQLite | IndexedDB(`judgments`,`meta`), 미지원 시 메모리 폴백 | 웹 표준 |
| 사진 식별 | asset_id | `파일명\|크기\|수정시각` FNV 지문 | 다시 골라도 판정 기록이 이어짐 |
| 서버 진행률 동기화 | Supabase | **미구현** | 로그인·서버 없음 |
| 스트릭 | 만들지 말 것 | 없음(화면에 명시) | 원안 유지 |

**선정 로직**(원안 그대로 유지) — 오래된 것 제곱 가중 씨앗 → ±180초 연속 촬영 묶음 확장 →
스크린샷 120장 이상이면 3일에 하루 스크린샷 트랙. 날짜 해시 시드라 같은 날엔 같은 다섯 장.

**검증** (`test-fivephotos.mjs`, 3만 장 더미 라이브러리)

- 3만 장 pool에서 5장 선정 **평균 4.1ms** (20회 평균, 목표 200ms 이하).
- 씨앗 위치 평균 **20.5%**(0%=최고참) → 오래된 것 우선 가중 확인.
- 같은 날·같은 pool → 같은 다섯 장(결정론), 다른 날 → 다른 다섯 장.
- 연속 촬영 묶음 동시 제시 경로 확인, 스크린샷 2,728장 인식, 60일 중 19일 스크린샷 트랙.
- 이미 판정한 사진 재출현 0건.
- ETA: 3만 장·하루 5장 → 2042년으로 계산(정직하게 노출).
- **업로드 경로 부재 정적 증명**: `fetch(`, `XMLHttpRequest`, `FormData`, `sendBeacon`,
  `WebSocket`, `.upload`, 캔버스 전송 패턴 전부 0건. 외부 요청은 애드센스 스크립트뿐.
  (프록시 캡처 대신 코드 부재 증명으로 대체 — 전송 코드가 없으면 캡처할 요청도 없다.)

---

## 3. 걸음의 모양 — `/walk-shape/`

**원안**: 모양 → OSM 보행 도로망 그래프에 정점 매핑 → 최단경로 연결 → Fréchet/Hausdorff로
유사도 채점 → 후보 3개 제시. 경로 계산은 서버(Node + graph 라이브러리).

**결정 — 경로 자동 생성은 구현하지 않았다**

이 앱의 심장인 "모양 → 도로망 경로 매칭"은 (1) OSM 추출본 저장소, (2) 그래프 최단경로 서버,
(3) 지도 타일 — 셋 다 서버·외부 API를 요구한다. 정적 사이트에서 성립하지 않으므로 **만들지
않았고, 만든 척도 하지 않았다.** 대신 서버 없이 가능한 세 가지로 재구성했다.

| 원안 | 구현 | 근거 |
|---|---|---|
| 모양 → 도로망 경로 생성 | **계획 계산**: 목표 거리(2~10km)를 넣으면 그림이 들어갈 가로·세로 실제 범위(m)를 산출 | 경로 대신 "크기"만 알려 줘도 즉흥 드로잉이 가능하다 |
| 지도 위 궤적 표시 | 지도 없이 궤적만 렌더 | 타일 = 외부 요청. 없애면 사용자 위치가 어떤 지도 서버에도 안 알려진다 |
| 후보 3개 제시 | 없음(경로가 없으므로) | — |
| Fréchet/Hausdorff 유사도 | **평활 → 정규화 → 양방향 p90+평균 + DTW(순서) + 길이 벌점** | 아래 참조 |
| 교차로 음성·진동 안내 | 없음 | 안내할 경로가 없다. 대신 "화면 보지 말라"는 안내를 남김 |
| 위험 도로 필터(ODbL) | **불필요** | 경로를 지시하지 않으므로 위험 도로로 안내할 가능성이 구조적으로 0. 대신 길 선택 책임이 사용자에게 있음을 상단 경고에 명시 |
| 갤러리 | 없음 | 서버 없음 |

**유사도 설계 — 왜 단순 하우스도르프를 쓰지 않았나**

1. 최댓값 하우스도르프만 쓰면 GPS가 한 번 튄 것만으로 0점이 된다 → **상위 10%(p90) + 평균**.
2. 거리만 보면 **같은 자리를 헤맨 낙서**가 도형 위를 지나가며 높은 점수를 받는다
   → **DTW(순서 정렬 비용)** 를 55% 비중으로 섞고, 닫힌 도형은 시작점 18방향 × 정·역방향 탐색.
3. GPS 고주파 잡음은 5점 이동평균으로 평활한 뒤 채점 → 잡음 ±0.06에도 93점.
4. 낙서 방지 보강: 궤적 길이가 모양 길이의 1.6배를 넘으면 길이 벌점.

**검증** (`test-walkshape.mjs`)

- **모양 판별력 22/22** — 22종 각각의 정확한 궤적이 22종 후보 중 자기 자신에게 최고점.
- 완벽 재현 96점, GPS 잡음 ±0.015 → 95점, ±0.06 → 93점(평활이 실제로 작동).
- 60%만 걸은 궤적 30점(덜 그린 만큼만 감점), 다른 모양 대조 46점.
- 무작위 산책 40회: 중앙값 35점, 65점 초과 2회(5%).
- 5km 고양이 → 가로 1,303m × 세로 1,567m, 거리 2배 → 범위 정확히 2배.
- 위경도→상대 미터 변환·haversine 오차 0.001° 기준 ±3m 이내.
- 1,500점 궤적 채점 **3.0ms**(DTW 36회 탐색 포함).
- 정적 검사: 외부 지도/도로망 API 0건, 백그라운드 위치 코드 0건,
  `watchPosition` 1회·`clearWatch` 2회(일시정지·종료), 결과 화면에 칼로리·페이스·심박 0건,
  공유 상태에 위도·경도 필드 0건.
- 사람 눈 대조용 **샘플 20종 이미지** 생성(`walk-shape-samples.svg`) — 목표 모양(초록) 위에
  걸은 궤적(흰색)과 점수를 나란히 배치.

---

## 4. 손글씨만 — `/handwriting/`

**원안**: 주제방이 있는 손글씨 전용 게시판. Next.js PWA + Supabase + OCR API,
전량 사람 검수 큐에서 시작.

**결정 — 공개 게시판을 만들지 않았다 (이번 축소 중 가장 큰 판단)**

기획서 스스로 "검수는 필수"라고 못 박았고, 손글씨 검수는 텍스트보다 훨씬 어렵다.
OCR + 이미지 분류 + 사람 검수 큐 = 서버 3종 세트인데 이 저장소엔 하나도 없다.
**검수 없는 공개 게시판을 여는 것은 무책임**하므로 게시판을 빼고 범위를
**링크로 주고받는 1:1 쪽지**로 좁혔다. 공개 유통면이 없으면 검수 대상도 없다.
"주제방·최신순 정렬·하루 1개 제한"은 게시판이 없으므로 함께 빠졌다.

| 원안 | 구현 | 근거 |
|---|---|---|
| 주제방 게시판 | **쪽지(링크 공유)** | 검수 불가 → 공개 유통면을 만들지 않음 |
| OCR 검수 / 이미지 분류 / 사람 큐 | 없음 | 외부 API·서버 금지 |
| 손글씨 답장 | **구현**(`?n=원본&a=답장`) | 답장도 손글씨여야 한다는 규칙은 유지 |
| 좋아요 버튼 | 없음 | 원안 유지 |
| 획 데이터 저장 | **구현** — 좌표 차분 + zigzag + varint + base64url | 서버 대신 링크가 저장소 |
| 재생 | **구현** — 배속 4단, 3초 이상 정지 → 0.5초 압축, 지운 획도 나타났다 사라짐 | 시그니처 기능 |
| 필압 | **미저장**(속도 기반 굵기만) | 용량. 화면에 명시 |
| alt 텍스트(OCR) | 불가 | 접근성 한계를 소개·FAQ·상단 배너에서 세 번 고지 |
| SEO | 시도하지 않음 | 원안 지시 그대로 |

**입력 지연 예산** — 목표 **25ms 이하**(화면 갱신 1프레임 16.7ms 안에 그리기).
설계상: `pointermove` 핸들러에서 **직전 구간만 증분 렌더**하고 전체 리페인트를 하지 않는다.
전체 리페인트는 획이 쌓일수록 느려져 지연의 주범이 된다. 실측치는 앱 화면의
"입력 지연 중앙값"에 상시 표시된다(`event.timeStamp` → `performance.now()` 차이의 중앙값).

**검증** (`test-handwriting.mjs`)

- 코덱 왕복 **무손실**(좌표·시각 최대 오차 0), 색·펜·지운 시각 보존.
- **1페이지 24획 / 1,080점 → 3,395B (3.32KB)**, 점당 3.14B. 목표 20KB 대비 6분의 1.
  base64 링크 4,527자로 한도 6,144자 이내.
- 재생 시간 압축: 실제 12.05초 → 재생 0.60초. 정지가 없으면 시간 그대로.
- 지운 획의 지운 시각이 재생 타임라인에 포함됨.
- 깨진 입력(null·매직바이트 불일치·잘못된 base64)에서 예외 없이 방어.
- 정적 규칙: `<input>` 0개, `<textarea>` 0개(복사 폴백 제외), `contenteditable` 0개,
  사진 업로드 경로 0개(`type="file"`/`FileReader` 없음), 서버 전송 코드 0개.
- **재생 샘플 3종** 이미지 생성(`handwriting-samples.svg`) — 획 데이터에서 복원,
  지운 획은 점선으로 표시.

---

## 공통 마무리

- 네 앱 모두 `gen-pages.mjs`의 `BUILTINS` + `BUILTIN_CATS`("📖 언어 · 기록")에 등록.
- `node gen-pages.mjs` 경고 0, `광고 영역`/`REPLACE_` 잔재 0, 광고 유닛은 결과 하단 1개.
- 외부 요청은 애드센스 스크립트뿐. 네 앱 모두 지도·폰트·CDN·API 호출 0건.
- 검색 색인은 보류(`CORE_SLUGS` 미등록 → `noindex,follow`). 실사용 데이터를 보고
  콘텐츠 품질이 확인되면 그때 하나씩 색인 해제하는 저장소 관례를 따랐다.

### 실제 브라우저 검증 (Chromium, 430×900 모바일 뷰포트)

정적 검사만으로는 "동작한다"를 말할 수 없어 네 앱 모두 실제 브라우저에서 조작했다.
**전 항목 통과, 콘솔 오류 0건.**

| 앱 | 확인한 것 |
|---|---|
| nameless | 등재어 30·미명 18 렌더, 명명→카드→`?r=` 기록, 집단지칭 이름 반려, 분야 필터, 공유 링크에서 결과 재현 |
| five-photos | 더미 12장 주입 → 판정 화면, 스와이프 판정 3종, 되돌리기, 하루 몫 완료, 유예함·진행률·ETA·결산 |
| walk-shape | 모양 22종, 5km 계획(1,303×1,567m), 고양이 궤적 그리기 → **96점**, 공유 URL에 위경도 없음 |
| handwriting | 입력 요소 0개, 획 기록·되돌리기·지우개, **입력 지연 중앙값 7ms**(목표 25ms), 링크 생성·수신·재생·답장 |

검증 중 발견해 고친 것:

- 헤드리스 Chromium이 `mousemove`를 프레임 단위로 병합해 50개 중 6개만 전달하는 것을 확인.
  실제 기기에서도 빠른 필기는 같은 방식으로 병합되므로 **handwriting의 `pointermove`에서
  `getCoalescedEvents()`로 합쳐진 원본 이벤트를 모두 꺼내 쓰도록 수정**했다.
  (이 문제로 뭉개진 궤적은 77점, 온전한 궤적은 96점 — 점수 차이로도 확인된다.)
- five-photos의 완주 연도 안내에서 "하루 15장" 문구가 같은 해일 때도 붙던 것을 조건부로 변경.
- nameless 사전 조판: 한글 세리프가 없는 기기에서 산세리프로 떨어지는 것을 확인하고
  폰트 스택을 넓힘(Noto Serif KR / Source Han Serif K 등 추가). 세리프가 없어도
  지면색·3중선 제호·자간·「」 용례로 사전 느낌이 유지되는 것을 스크린샷으로 확인.
- handwriting 펜 굵기를 모바일 기준으로 상향(2.6/5.0/8.5 → 3.4/6.2/10.5).


---

# 만족도 리뷰 루프 (게이트 90 · 유저 10명)

`REVIEW-LOOP.md`의 방식을 따르되 게이트를 **80 → 90**, 평가자를 **5명 → 10명**으로 올려
네 앱을 4라운드 돌렸다. 점수를 지어내지 않기 위해 매 라운드 **실제 브라우저 계측**을 먼저 하고
(360·430px 가로 오버플로우/터치타깃/폰트/콘솔오류/첫화면 조작요소), 그 결함 위에 페르소나 점수를 매겼다.

**유저 10명**: 20대 모바일 헤비 · 40대 실용주의 · 디자인 민감 · 도메인 전문가 · 첫방문 캐주얼 ·
접근성 필요(저시력/키보드) · 프라이버시 민감 · 저사양 안드로이드 · 데스크톱 · 공유(SNS) 관점.

## 라운드별 유저 평균

| 앱 | 1R | 2R | 3R | 4R | 최종 overall |
|---|---|---|---|---|---|
| 이름 없는 것들 | 84.3 | 89.3 | 90.2 | 90.2 | **90.3** |
| 오늘의 다섯 장 | 80.5 | 89.1 | 90.2 | 90.2 | **90.0** |
| 걸음의 모양 | 83.4 | 88.5 | 89.9 | 90.9 | **90.8** |
| 손글씨만 | 84.0 | 87.5 | 89.0 | 90.2 | **90.1** |
| **평균** | **83.1** | **88.6** | **89.8** | **90.4** | **90.3** |

## 라운드별로 실제 고친 것

**1R — 계측 결함 제거**: 터치타깃 30~32px → 44px(네 앱 전부), 11.5px 텍스트 → 12.5px 이상,
five-photos 360px 첫 화면에 시작 버튼 노출(안내를 한 줄 배지 + 접이식으로), nameless 초성 색인.

**2R — 실사용을 막던 것**: walk-shape Wake Lock(걷는 중 화면 꺼짐 방지)과 궤적 localStorage
저장·복원(소개에 있던 "여러 날 완성"을 실제 기능으로), five-photos EXIF 촬영일·키보드 단축키·
프리로드, handwriting 여러 장 저장·링크 한도 잠금·재생 정지.

**3R — 쓸 이유 보강**: walk-shape 모양 회전 4방향(회전한 모양이 채점 기준)·북쪽 표시·소요 시간,
nameless 상호참조 60개·미명 번호, five-photos 기간별 결산과 비율 막대, handwriting 크게 보기와 사용 사례.

**4R — 마지막 두 앱의 약점**: walk-shape **GPX 불러와 채점**(데스크톱 활용처 + 전문가 니즈)과
결과 캔버스 aria-label, handwriting **되살리기·필기 보정·고대비 확대**(저시력/마우스 사용자).

## 루프 중 테스트가 잡은 실제 버그

- walk-shape: 저장된 궤적을 복원한 뒤 **바로 마무리할 방법이 없었다**(종료 버튼이 숨겨져 있었음).
- handwriting: `.tools button`(요소 선택자 포함)이 `.swatch`의 `width`를 눌러 색 버튼이 30px로 축소.
- five-photos: 완주 연도 안내에서 "하루 15장" 문구가 같은 해일 때도 붙던 것.

## 남은(고치지 않기로 한) 한계

전부 서버가 없어서 생기는 구조적 한계이고, 앱 화면에 그대로 적어 두었다.
집단 표결·공용 등재(nameless) / 사진 삭제·아침 알림(five-photos) / 경로 자동 생성·지도(walk-shape) /
공개 게시판·OCR 대체텍스트(handwriting).

기록: `data/review-scores.json`(게이트 90, loops 4, 10인 점수 원본 포함) → `/review-dash/`에 표시.
