{
  "name": "ugc-platform",
  "version": "0.0.0",
  "private": true,
  "description": "Standalone workspace for @ggu/ugc-core and its consumer services. Isolated from the tomatoeggcat static site — the static deploy (gen-pages.mjs) never touches this directory.",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "typecheck": "npm run typecheck --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  }
}
