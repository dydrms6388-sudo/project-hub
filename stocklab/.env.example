# ── Supabase (없으면 샘플 데이터 모드로 동작) ──
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# 서버 전용 (usage_limits 기록, daily_picks 적재). 브라우저에 노출 금지.
SUPABASE_SERVICE_ROLE_KEY=

# ── 사이트 ──
NEXT_PUBLIC_SITE_URL=https://stocklab.tomatoeggcat.com
NEXT_PUBLIC_ADSENSE_CLIENT=ca-pub-5567719201265106

# ── 크론 보호 (Vercel Cron / GitHub Actions → /api/cron/daily-pick) ──
CRON_SECRET=

# ── 파이프라인(Python) 전용 — pipeline/README.md 참고 ──
DART_API_KEY=
KIS_APP_KEY=
KIS_APP_SECRET=
