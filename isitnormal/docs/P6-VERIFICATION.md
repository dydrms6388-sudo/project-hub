# P6 검증 루프 — 적대적 심사 결과

이진 판정. 점수제 금지. 자동 게이트는 `scripts/verify.ts`(`npx tsx scripts/verify.ts`)로 재현.

## 종료 요약 (2회 루프에 통과)

| 루프 | 항목 | 결과 | 실패건수 | 조치 |
|---|---|---|---|---|
| 1 | A4 sitemap 1,200자 미만 | ❌ | 홈+정보페이지 6 | 홈에 고유 본문 700자+ 추가, 정보/약관 페이지 sitemap 제외 |
| 1 | B6 이탈 이유 | ⚠ | 1 (주간 신선도 부재) | 데이터 게이트 이유로 의도적 연기(아래 주석), 백로그 등록 |
| 1 | E2 시크릿창 재투표 | ❌ | 1 | dedup 키를 session 제외 ip_hash+fingerprint로 변경 + unique 인덱스 교체 |
| 1 | F7 고아 페이지 | ❌ | 1 (/submit) | footer에 링크 추가 |
| 2 | A~F 전체 재실행 | ✅ | 0 | — |

## A. 애드센스 심사관 (색인 대상 = 홈 + 허브 12)

- A1 없어도 아쉬울 것 없는 페이지? → **N**(탈락 아님). 허브마다 고유 1,200자+ 편집 본문 + 5개 인터랙티브 설문 + FAQ.
- A2 이 페이지에만 있는 정보 3개 인용 가능? → **Y**. 카테고리별 구체 장면·논점 서술.
- A3 문장구조 80% 동일? → **N**. 해설 pairwise 유사도 최대 0.020(F8).
- A4 sitemap 1,200자 미만 1개라도? → **N**. 홈(고유본문 보강) + 허브 12 전부 1,200자+.
- A5 미승격 UGC 색인? → **N**. 시드/UGC 설문 전부 noindex + sitemap 제외. 투표 0이라 승격 게이트 자동 차단.
- 판정: **합격**.

## B. 6인 패널

- B1 3초 안에 뭐하는 곳? ✅ 히어로 "우리집만 이래? 익명 투표로 1탭에".
- B2 1탭 참여? ✅ 홈 "오늘의 질문" 즉시 투표.
- B3 표본수·집계 방식 확인? ✅ StatsResult "표본 n명", about/disclaimer 설명.
- B4 재방문 얻을 것? ⚠ **주간 통계 페이지(R3)는 투표 데이터가 쌓인 뒤 발행**(0표 상태에서 열면 빈 목록 = S3 위반). 데이터 게이트 연기.
- B5 저사양/LTE/확대200%? ✅ 상대 단위·저용량 JS(102KB)·고정폭 없음.
- B6 이탈 이유 1개? → "주간 신선도 부재" 등록 → B4 연기로 대응.
- 판정: **5/6 합격**.

## C. 바이럴 심사관

- C1 카톡에 올리면 창피? → **N**. 깔끔한 카드.
- C2 맥락 없이 이해? → **Y**. 제목+해석 문구.
- C3 나에 대한 정보? → **Y**. "상위 4% 소수파" 등 페르소나(숫자 단독 아님).
- C4 12종 어미만 다른가? → **N**. 문장구조·프레이밍 상이(헤드라인 12개 고유).
- C5 뿌릴 이유 한 문장? → "내가 소수파라는 게 신기해서 스스로 뿌린다".
- C6 /s/ 색인? → **N**. noindex 헤더 + 메타 + robots Disallow 삼중.
- 판정: **합격**.

## D. 커뮤니티/법무 심사관

- D1 0개 섹션 노출? → **N**. 시드 60 상시 존재, 빈 목록 자리엔 CTA(S3).
- D2 시드 유저인 척? → **N**. 전부 @운영자 배지·origin=operator.
- D3 n<30 %노출? → **N**. showStats 게이트로 "집계 중".
- D4 PII 10종 통과율? → **0%**.
- D5 스팸 10종 통과율? → **0%**.
- D6 신고→숨김 / takedown 실동작? → **Y**. 신고 3건 자동 held, takedown 48h 큐.
- 판정: **합격**.

## E. 어뷰징 레드팀

- E1 같은 기기 재투표? → **차단**. ip_hash+fingerprint unique + 앱 선검사.
- E2 시크릿창 반복 투표? → **차단**(루프1 수정). dedup 키에서 session 제외.
- E3 익명 pending select? → **불가**. RLS: anon SELECT는 status='approved'만.
- E4 rate limit 우회? → IP 1일 투표 50 / 작성 3 상한. fingerprint 위조해도 IP 상한이 캡.
- 판정: **합격**.

## F. 기술 게이트

| 항목 | 결과 |
|---|---|
| F1 build/type 에러 0 | ✅ tsc 0, next build 87페이지 |
| F2 Lighthouse Perf90/SEO100/A11y95/BP95 | ⏳ **배포 후 측정**(First Load 102KB·시맨틱·lang=ko·ad-slot min-height로 CLS 대비·aria 라벨) |
| F3 sitemap URL == 색인 대상 | ✅ 13 == 홈1+허브12 |
| F4 JSON-LD 유효 / 미검증 UGC Rating 0 | ✅ Rating/Review 스키마 0, BreadcrumbList·FAQPage·WebSite만 |
| F5 중복 title·description·H1 0 | ✅ |
| F6 1,200자 미만 색인 페이지 0 | ✅ 허브 1,242~1,523 / 홈 보강 / 해설 300자+ |
| F7 깨진 링크·고아 0 | ✅ related 전부 유효, /submit footer 링크 |
| F8 해설 유사도 0.7 초과 0 | ✅ 최대 0.020 |
| F9 AI 상투구 0 | ✅ |

## 남은 데이터 게이트 항목 (트래픽 발생 후)

- 주간 통계 해설 페이지(R3) 발행 — 투표 누적 후.
- 시드 승격(색인 전환) — n≥30 + 7일 경과분에 한해 승격 잡 실행, sitemap 편입.
- Lighthouse 실측 — 배포 후.
